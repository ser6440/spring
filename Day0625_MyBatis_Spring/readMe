Dependency 등록
Spring : core, context, jdbc
MyBatis : mybatis, mybatis-spring
oracle : ojdbc

실습 목적:
MyBatis를 이용해서 만든 DAO를 스프링 컨테이너에 bean으로 등록해서 사용하기

1. bean 으로 등록해야 할 목록
   dataSource : DB접속정보 저장
   sqlSessionFactory : sqlSession을 만들기 위함
   : >> 설정정보(DB접속 정보, alias, mapper 위치정보)가 필요함